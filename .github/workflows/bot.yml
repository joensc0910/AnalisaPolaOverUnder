name: Bot Pengecek Bola Harian

on:
  schedule:
    - cron: '0 0 * * *' # Berjalan setiap jam 00:00 UTC (07:00 WIB Pagi)
  workflow_dispatch: # Tombol manual untuk mencoba bot kapan saja

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Jalankan Script Utama
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GCP_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS }}
        run: python main.py

      - name: Jalankan Script Pengecek Bola
        run: python bot.py

      - name: Lapor Error ke Telegram
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} -d text="⚠️ ALARM: Mesin Pengecek Bola hari ini GAGAL berjalan! Segera cek log di GitHub."
